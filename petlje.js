// PETLJE //

/*
const uraditi = [
	{
		id : 1,
		tekst : 'Izbaci smece',
	},
	{
		id : 2,
		tekst: 'Uradi zadacu',
	}
]


for(let i = 0; i <= 10; i++){ // for petlja
  console.log('Broj petlje : ${i}');
}

// while petlja
let i = 0 // - razlika u while i for je sto damo vrijednost izvan petlje
while(i <= 10) {
  console.log(i);
  i++;
}

//petlje kroz nizove (nije najbolji nacin)

for(let i = 0; i <= uraditi.length; i++){ 
  console.log(uraditi[i].tekst);
}

//ovo je bolji nacin loopanja kroz nizove

for(let uradi of uraditi) {
	console.log(uradi.tekst)
}

// metode sa nizovima sa visom vrijednoscu

// forEach() - loopa se kroz niz
todos.forEach(function(uradi) { // uzimaju funkciju kao perimetar

  console.log({uradi.text});

  console.log(myTodos);
});

// map() - pravi novi niz od postojeceg
const uraditekst = uraditi.map(function(uradi) { // ovdje vracamo samo vrijednost tekstać

  return uradi.text;
});

console.log(uraditekst);

// filter() - Returns array based on condition
const todo1 = uraditi.filter(function(uradi) {
  
  return uradi.id === 1; //- vraca id gjde je samo 1 (u ovom slucaju)
});

*/