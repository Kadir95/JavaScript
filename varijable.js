// VARIJABLE //
/* let, const; (let može promjeniti zadanu vrijednost dok const(constant) nema mogucnost mijenjanja vrijednosti)
 
  let age= 30;
 age =31;
 console.log (age) */

// stringovi, brojevi, nullte vrijednosti, boolean, nedefisani i simboli su vrijednosti koje dajemo Varijablama //

 /* const name = 'John'; 
const age = 30;
const rating = 3.5; - nema floata u Javi, ovo je samo broj 
const isCool = true; - ovo je boolean, pise se bez navodnika jer sa navodnicima je string
const x = null;
const y = undefined;
let z; - ovo je također undefined jer nema dodjeljenju vrijednost

console.log(typeof isCool); - type of ce nam u consoli izbaciti sta je tip varijable koje smo naveli
 */

/*
// concatenation
const name = 'Kadir';
const age = 16;
console.log('Moje ime je  ' + name +  ' i imam ' + age + '  godina'); - stari nacin ubacivanja vrijednosti u konzolu

 template string

console.log(' Moje ime je ${name} i imam ${16} godina ');  - jedan nacin template stringa gjde stavljamu sintaksu; znak novca i ove zagrade

const zdravo = 'Moje ime je ${name} i imam ${age} godina '; console.log(zdravo); - drugi nacin

//metode sa stringovima
const s='Zdravo svijete?!?!?';

console.log(s.length) - length broji koliko znakova ima u stringu
console.log(s.toUpperCase()) - ovje koristimo METODU; metoda- funkcija koja se asocira na neki objekat
console.log(s.substring(0, 6).toUpperCase());


*/

